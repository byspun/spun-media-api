import { attachSubtitles } from '../normalizer.js';
import { detectFormat, isSafeHttpUrl, normalizeQuality } from '../mapper.js';
import type { AnimeProviderInput, RawStream, RawSubtitle } from '../shared/types.js';
const BASE='https://anime-dunya.com';
export async function getAnimedunyaStreams(input: AnimeProviderInput): Promise<RawStream[]> { try { const r=await fetch(`${BASE}/api/watch/${input.anilist_id}/${input.episode}`,{headers:{Accept:'application/json'},signal:AbortSignal.timeout(20_000)}); const p:any=await r.json(); const values=Array.isArray(p?.streams)?p.streams:Array.isArray(p?.sources)?p.sources:[]; const subs=(Array.isArray(p?.subtitles)?p.subtitles:[]).filter((x:any)=>isSafeHttpUrl(x.url)).map((x:any):RawSubtitle=>({url:x.url,language:String(x.language??'Unknown'),language_code:String(x.language_code??x.lang??'und').toLowerCase(),format:'vtt',provider:'animedunya'})); const streams:RawStream[]=values.filter((x:any)=>isSafeHttpUrl(x.url)&&!['embed','hls-redirect'].includes(String(x.type))).map((x:any)=>({url:x.url,format:detectFormat(x.url,x.type) as any,quality:normalizeQuality(x.quality??x.label),audio:'Japanese',provider:'animedunya'})).filter(x=>x.format!=='unknown'); return attachSubtitles(streams,subs); } catch{return [];}}
