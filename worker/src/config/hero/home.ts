// worker/src/config/hero/home.ts
// Manual override for the /home hero carousel — all content types mixed.
// Items here appear FIRST in the hero, in this exact order.
// Algorithmic picks fill remaining slots up to a max of 7.
// Leave empty for fully algorithmic.

export const homeHero: Array<{ spun_id: string; note: string | null }> = [
  // { spun_id: 'your-title-xxxxxx', note: 'Pin reason — remove when done' },
];
